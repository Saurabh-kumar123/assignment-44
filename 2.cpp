//2. Using inbuilt function and insert an element to the set container.
#include<iostream>
#include<iterator>
#include<set>
using namespace std;
int main()
{
     set<int> s;
     s1.insert(10);
      s1.insert(70);
      s1.insert(20);
      s1.insert(50);
      s1.insert(40);
      s1.insert(30);

     return 0;
}
